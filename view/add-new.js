import { LitElement, html, css } from "lit";

class AddNew extends LitElement {
  static styles = [css``];

  render() {
    return html` <div>AddNew</div>`;
  }
}

customElements.define("add-new", AddNew);
